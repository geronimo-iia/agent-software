# Rust Development Standards Auto-Activation

## Activation Triggers

When working with:
- Rust source files (`*.rs`, `src/` directories)
- Cargo configuration (`Cargo.toml`, `Cargo.lock`)
- Rust tooling configs (`clippy.toml`, `rustfmt.toml`)
- Rust CI workflows and build scripts
- Discussions about Rust project structure, testing, or tooling

## Auto-Include Content

Include the following agent-software standards as context:
- `agent-software/rust/definition.md`
- `agent-software/rust/project-structure-tools.md`
- `agent-software/rust/ci-cd.md`

## Purpose

Ensures consistent Rust development practices including project structure, tooling configuration, CI/CD setup, and code quality standards.

## Scope

Applies to all Rust projects in the workspace, from CLI tools to libraries and WASM components.